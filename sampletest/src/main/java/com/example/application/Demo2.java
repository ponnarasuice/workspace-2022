package com.example.application;

public class Demo2 {
  Node head;

  public Demo2(Node head) {
    this.head = null;
  }

  private void insertHead(int value) {
    // if head == null, new node
    // if hea !=null , head.next = temo,
    // remove middle node --- check counter for node size. node
  }
}
